{
  "routes": [
    {
      "method": "GET",
      "path": "/incidents",
      "handler": "incidents.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/incidents/count",
      "handler": "incidents.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/incidents/:id",
      "handler": "incidents.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/incidents",
      "handler": "incidents.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/incidents/:id",
      "handler": "incidents.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/incidents/:id",
      "handler": "incidents.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
